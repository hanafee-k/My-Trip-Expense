/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',  // 👈 เพิ่มบรรทัดนี้สำคัญมาก! (ทำให้เป็นไฟล์ HTML ธรรมดา)
  images: { unoptimized: true } // 👈 เผื่อมีการใช้รูปภาพ จะได้ไม่ Error
};

export default nextConfig;